{
 "_comment": "Where published thumbnails are served from. Not a secret — the deploy runner reads this to build image URLs, while uploads happen locally. r2_bucket lets publish fall back to wrangler when S3 credentials aren't set.",
 "r2_public_base": "https://pub-f75027cb6c72486eab43e77b500919ca.r2.dev",
 "r2_prefix": "thumbs",
 "r2_bucket": "sears-island-flora",
 "_contributor_comment": "Where a signed-in contributor submits field checks, surplus marks and photographs. Not a secret — the deploy runner reads it to build the page, exactly like r2_public_base, and holds no credential either way. Leave it empty and the site is the read-only survey it was before. Set it to the deployed Worker's URL, e.g. https://sears-island-contributors.<subdomain>.workers.dev",
 "contributor_endpoint": "",
 "survey_area": {
  "name": "Sears Island",
  "_comment": "Approximate bounding box covering the island, its causeway and immediate shoreline — deliberately generous, so widen or tighten as you learn the ground. enforce: 'auto' means the check stays advisory until the first in-area record arrives, then becomes binding.",
  "enforce": "auto",
  "lat_min": 44.43,
  "lat_max": 44.51,
  "lon_min": -68.93,
  "lon_max": -68.84
 }
}
